<% if current_user %>
     <div class="col-md-8">
       <%= render partial: 'items/item', collection: current_user.items %>
       <%= render 'items/form', item: Item.new, user: @user %>
    <% end %>
    </div>

